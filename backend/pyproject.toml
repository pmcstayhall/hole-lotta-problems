[project]
name = "road-hazard-backend"
version = "0.1.0"
description = "Road Hazard Alert & Navigation System — Flask backend"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "flask-cors>=4.0",
    "shapely>=2.0",
    "pyproj>=3.6",
]

[tool.uv]
package = false
